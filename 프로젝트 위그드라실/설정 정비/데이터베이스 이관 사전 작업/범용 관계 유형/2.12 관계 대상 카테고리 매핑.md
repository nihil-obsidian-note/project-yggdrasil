# 2.12 관계 대상 카테고리 매핑

> 상태: 범용 기준 확정 매핑  
> 범위: 2.12 재료·구성·생산 관계 55개와 대상 역할 215개  
> 적용 범위: `COMMON`

## 1. 적용 원칙

- 범용 카테고리는 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형의 11개다.
- 하위 카테고리는 해당 메인 카테고리 허용에 포함한다.
- 선택 역할도 값이 입력되면 동일한 검증을 적용한다.
- `전체 11개` 허용 역할은 없으며, 모든 역할을 단일 또는 복수 제한으로 판정했다.
- 룩스테라 용종과 엘드로스 클래스는 범용 매핑에 포함하지 않는다.

## 2. 관계별 대상 역할 매핑

| 전역 ID | 2.12 파트 | 관계 | 대상 수 | 대상 역할별 카테고리 |
| ---: | --- | --- | ---: | --- |
| 787 | 2.12-1 | `구성 요소임` ↔ `구성 요소를 가짐` | 2~3 | 1 `구성 요소` → 개념·종족·아이템<br>2 `전체 대상` → 신격·도시·종족·인물·아이템·지형<br>3 `관련 형태·시기`(선택) → 개념·사건 |
| 788 | 2.12-1 | `부품임` ↔ `부품을 가짐` | 2~3 | 1 `구성 부품` → 아이템<br>2 `완성 대상` → 도시·아이템·지형<br>3 `관련 형식·세대`(선택) → 개념·아이템 |
| 789 | 2.12-1 | `재료임` ↔ `재료로 구성됨` | 2~3 | 1 `구성 재료` → 개념·종족·아이템<br>2 `결과물` → 신격·도시·종족·인물·아이템·지형<br>3 `관련 구조·배합·공정`(선택) → 개념·사건 |
| 790 | 2.12-1 | `원료임` ↔ `원료로 사용함` | 2~4 | 1 `생산 원료` → 개념·종족·아이템<br>2 `생산 결과물` → 개념·종족·아이템<br>3 `관련 생산 공정`(선택) → 개념·사건<br>4 `생산 주체·시설`(선택) → 신격·국가·도시·단체·종족·인물·아이템 |
| 791 | 2.12-1 | `성분임` ↔ `성분을 함유함` | 2~3 | 1 `함유 성분` → 개념·종족·아이템<br>2 `함유 대상` → 신격·종족·인물·아이템·지형<br>3 `관련 함량·배합·공정`(선택) → 개념·사건 |
| 792 | 2.12-1 | `첨가물임` ↔ `첨가물을 함유함` | 2~3 | 1 `첨가물` → 개념·종족·아이템<br>2 `첨가 대상` → 개념·종족·아이템<br>3 `첨가 목적·공정`(선택) → 개념·사건 |
| 793 | 2.12-1 | `연료임` ↔ `연료로 사용함` | 2~4 | 1 `연료` → 개념·종족·아이템<br>2 `사용 주체·시설` → 국가·도시·단체·종족·인물·아이템<br>3 `가동 대상`(선택) → 개념·도시·아이템·지형<br>4 `관련 시기`(선택) → 개념·사건 |
| 794 | 2.12-1 | `촉매임` ↔ `촉매로 사용함` | 2~4 | 1 `촉매` → 개념·종족·아이템<br>2 `대상 공정·반응` → 개념·사건<br>3 `사용 주체`(선택) → 신격·단체·종족·인물·아이템<br>4 `결과물`(선택) → 개념·종족·아이템 |
| 795 | 2.12-1 | `부산물임` ↔ `부산물을 산출함` | 2~4 | 1 `부산물` → 개념·종족·아이템<br>2 `산출 주체·시설` → 국가·도시·단체·종족·인물·아이템·지형<br>3 `주 생산물`(선택) → 개념·종족·아이템<br>4 `관련 공정·사건`(선택) → 개념·사건 |
| 796 | 2.12-2 | `채굴함` ↔ `채굴됨` | 2~4 | 1 `채굴 주체` → 국가·단체·종족·인물<br>2 `채굴 자원` → 아이템·지형<br>3 `채굴지`(선택) → 국가·도시·지형<br>4 `관련 시설·사건`(선택) → 도시·아이템·사건·지형 |
| 797 | 2.12-2 | `채석함` ↔ `채석됨` | 2~4 | 1 `채석 주체` → 국가·단체·종족·인물<br>2 `석재 자원` → 아이템·지형<br>3 `채석장`(선택) → 도시·지형<br>4 `관련 시설·사건`(선택) → 도시·아이템·사건·지형 |
| 798 | 2.12-2 | `벌목함` ↔ `벌목됨` | 2~4 | 1 `벌목 주체` → 국가·단체·종족·인물<br>2 `수목·목재 자원` → 종족·아이템<br>3 `벌목지`(선택) → 국가·도시·지형<br>4 `관련 사건`(선택) → 사건 |
| 799 | 2.12-2 | `채집함` ↔ `채집됨` | 2~4 | 1 `채집 주체` → 국가·단체·종족·인물<br>2 `채집 자원` → 종족·아이템<br>3 `채집지`(선택) → 국가·도시·지형<br>4 `관련 시기`(선택) → 개념·사건 |
| 800 | 2.12-2 | `채취함` ↔ `채취됨` | 2~4 | 1 `채취 주체` → 신격·국가·단체·종족·인물<br>2 `채취 자원·시료` → 개념·종족·아이템<br>3 `채취원·장소`(선택) → 신격·국가·도시·종족·인물·아이템·지형<br>4 `관련 목적·사건`(선택) → 개념·사건 |
| 801 | 2.12-2 | `수확함` ↔ `수확됨` | 2~4 | 1 `수확 주체` → 국가·단체·종족·인물<br>2 `수확물` → 종족·아이템<br>3 `재배지·생산지`(선택) → 국가·도시·아이템·지형<br>4 `수확 시기`(선택) → 개념·사건 |
| 802 | 2.12-2 | `어획함` ↔ `어획됨` | 2~4 | 1 `어획 주체` → 국가·단체·종족·인물<br>2 `어획 생물·자원` → 종족·아이템<br>3 `어장`(선택) → 국가·도시·지형<br>4 `관련 시기·수단`(선택) → 개념·아이템 |
| 803 | 2.12-2 | `사냥함` ↔ `사냥당함` | 2~4 | 1 `사냥 주체` → 국가·단체·종족·인물<br>2 `사냥감` → 종족<br>3 `사냥터`(선택) → 국가·도시·지형<br>4 `관련 목적·사건`(선택) → 개념·사건 |
| 804 | 2.12-2 | `표본을 채취함` ↔ `표본으로 채취됨` | 2~4 | 1 `표본 채취 주체` → 신격·국가·단체·종족·인물<br>2 `표본 대상` → 신격·종족·인물·아이템·지형<br>3 `채취원·장소`(선택) → 국가·도시·종족·인물·아이템·지형<br>4 `관련 연구·사건`(선택) → 개념·단체·사건 |
| 805 | 2.12-2 | `표본화함` ↔ `표본화됨` | 3~4 | 1 `표본화 주체` → 국가·단체·종족·인물<br>2 `원 시료·대상` → 신격·종족·인물·아이템·지형<br>3 `결과 표본` → 아이템<br>4 `관련 연구·보관처`(선택) → 개념·도시·단체·아이템 |
| 806 | 2.12-2 | `도축함` ↔ `도축됨` | 2~4 | 1 `도축 주체` → 국가·단체·종족·인물<br>2 `도축 생물` → 종족<br>3 `생산물`(선택) → 아이템<br>4 `관련 시설·사건`(선택) → 도시·아이템·사건·지형 |
| 807 | 2.12-3 | `재배함` ↔ `재배됨` | 2~4 | 1 `재배 주체` → 국가·단체·종족·인물<br>2 `재배 생물·작물` → 종족<br>3 `재배지`(선택) → 국가·도시·아이템·지형<br>4 `관련 품종·시기`(선택) → 개념·종족 |
| 808 | 2.12-3 | `사육함` ↔ `사육됨` | 2~4 | 1 `사육 주체` → 국가·단체·종족·인물<br>2 `사육 생물` → 종족<br>3 `사육지·시설`(선택) → 국가·도시·아이템·지형<br>4 `관련 목적·시기`(선택) → 개념·사건 |
| 809 | 2.12-3 | `양식함` ↔ `양식됨` | 2~4 | 1 `양식 주체` → 국가·단체·종족·인물<br>2 `양식 생물` → 종족<br>3 `양식장·수역`(선택) → 국가·도시·아이템·지형<br>4 `관련 품종·시기`(선택) → 개념·종족 |
| 810 | 2.12-3 | `가축화함` ↔ `가축화됨` | 2~4 | 1 `가축화 주체·공동체` → 국가·단체·종족·인물<br>2 `대상 생물종` → 종족<br>3 `관련 지역`(선택) → 국가·도시·지형<br>4 `관련 시기·사건`(선택) → 개념·사건 |
| 811 | 2.12-3 | `배양함` ↔ `배양됨` | 2~4 | 1 `배양 주체` → 국가·단체·종족·인물·아이템<br>2 `배양 생물·조직·물질` → 개념·종족·아이템<br>3 `배양 기반·시설`(선택) → 도시·아이템·지형<br>4 `결과물`(선택) → 개념·종족·아이템 |
| 812 | 2.12-3 | `육종함` ↔ `육종됨` | 2~4 | 1 `육종 주체` → 국가·단체·종족·인물<br>2 `대상 생물·품종` → 종족<br>3 `목표 특성`(선택) → 개념<br>4 `결과 품종·계통`(선택) → 종족 |
| 813 | 2.12-3 | `교배함` ↔ `교배됨` | 3~4 | 1 `교배 주체` → 국가·단체·종족·인물<br>2 `교배 개체·계통 1` → 종족<br>3 `교배 개체·계통 2` → 종족<br>4 `결과 개체·계통`(선택) → 종족 |
| 814 | 2.12-3 | `번식시킴` ↔ `번식 관리 대상임` | 2~4 | 1 `번식 관리 주체` → 국가·단체·종족·인물<br>2 `번식 대상 생물` → 종족<br>3 `번식지·시설`(선택) → 국가·도시·아이템·지형<br>4 `결과 개체·계통`(선택) → 종족 |
| 815 | 2.12-4 | `가공함` ↔ `가공됨` | 2~4 | 1 `가공 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `가공 대상` → 개념·종족·아이템<br>3 `가공 결과물`(선택) → 개념·종족·아이템<br>4 `관련 공정·시설`(선택) → 개념·도시·아이템·사건·지형 |
| 816 | 2.12-4 | `정제함` ↔ `정제됨` | 2~4 | 1 `정제 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `불순 원료·물질` → 개념·종족·아이템<br>3 `정제 결과물`(선택) → 개념·종족·아이템<br>4 `관련 공정·시설`(선택) → 개념·도시·아이템·사건·지형 |
| 817 | 2.12-4 | `제련함` ↔ `제련됨` | 2~4 | 1 `제련 주체` → 국가·단체·종족·인물·아이템<br>2 `광석·금속 원료` → 아이템<br>3 `제련 금속`(선택) → 아이템<br>4 `관련 시설·연료`(선택) → 개념·도시·아이템·지형 |
| 818 | 2.12-4 | `주조함` ↔ `주조됨` | 3~4 | 1 `주조 주체` → 국가·단체·종족·인물·아이템<br>2 `주조 재료` → 아이템<br>3 `주조 결과물` → 아이템<br>4 `관련 주형·시설`(선택) → 도시·아이템·지형 |
| 819 | 2.12-4 | `단조함` ↔ `단조됨` | 3~4 | 1 `단조 주체` → 국가·단체·종족·인물·아이템<br>2 `단조 재료` → 아이템<br>3 `단조 결과물` → 아이템<br>4 `관련 시설·도구`(선택) → 도시·아이템·지형 |
| 820 | 2.12-4 | `분쇄함` ↔ `분쇄됨` | 2~4 | 1 `분쇄 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `분쇄 대상` → 종족·아이템<br>3 `분쇄 결과물`(선택) → 아이템<br>4 `관련 장치·시설`(선택) → 도시·아이템·지형 |
| 821 | 2.12-4 | `용융시킴` ↔ `용융됨` | 2~4 | 1 `용융 주체·원인` → 개념·신격·국가·단체·종족·인물·아이템·사건<br>2 `용융 대상` → 아이템<br>3 `용융 결과물`(선택) → 아이템<br>4 `관련 시설·열원`(선택) → 개념·도시·아이템·지형 |
| 822 | 2.12-4 | `용해함` ↔ `용해됨` | 3~4 | 1 `용해 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `용해 대상` → 개념·종족·아이템<br>3 `용매·매질` → 개념·아이템<br>4 `결과 용액`(선택) → 아이템 |
| 823 | 2.12-4 | `증류함` ↔ `증류됨` | 2~4 | 1 `증류 주체` → 국가·단체·종족·인물·아이템<br>2 `증류 원료` → 개념·종족·아이템<br>3 `증류 결과물`(선택) → 개념·아이템<br>4 `관련 시설·공정`(선택) → 개념·도시·아이템·지형 |
| 824 | 2.12-4 | `발효함` ↔ `발효됨` | 2~4 | 1 `발효 주체·미생물` → 단체·종족·인물·아이템<br>2 `발효 원료` → 종족·아이템<br>3 `발효 결과물`(선택) → 아이템<br>4 `관련 시설·시기`(선택) → 개념·도시·아이템·지형 |
| 825 | 2.12-4 | `숙성함` ↔ `숙성됨` | 2~4 | 1 `숙성 주체·시설` → 단체·인물·아이템<br>2 `숙성 대상` → 종족·아이템<br>3 `숙성 결과물`(선택) → 아이템<br>4 `관련 기간·환경`(선택) → 개념·도시·지형 |
| 826 | 2.12-4 | `건조 처리함` ↔ `건조 처리됨` | 2~4 | 1 `건조 처리 주체·시설` → 신격·국가·단체·종족·인물·아이템<br>2 `건조 처리 대상` → 종족·아이템<br>3 `건조 결과물`(선택) → 아이템<br>4 `관련 환경·공정`(선택) → 개념·도시·아이템·지형 |
| 827 | 2.12-4 | `혼합함` ↔ `혼합됨` | 3~4 | 1 `혼합 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `혼합 재료 1` → 개념·종족·아이템<br>3 `혼합 재료 2` → 개념·종족·아이템<br>4 `혼합 결과물`(선택) → 개념·종족·아이템 |
| 828 | 2.12-4 | `배합함` ↔ `배합됨` | 3~4 | 1 `배합 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `배합 재료 1` → 개념·종족·아이템<br>3 `배합 재료 2` → 개념·종족·아이템<br>4 `배합 결과물`(선택) → 개념·종족·아이템 |
| 829 | 2.12-4 | `합성함` ↔ `합성됨` | 3~4 | 1 `합성 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `합성 원료` → 개념·종족·아이템<br>3 `합성 결과물` → 개념·종족·아이템<br>4 `관련 공정·촉매`(선택) → 개념·아이템·사건 |
| 830 | 2.12-4 | `추출함` ↔ `추출됨` | 3~4 | 1 `추출 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `추출원` → 개념·신격·종족·인물·아이템·지형<br>3 `추출물` → 개념·종족·아이템<br>4 `관련 공정·시설`(선택) → 개념·도시·아이템·사건·지형 |
| 831 | 2.12-4 | `분리함(물질)` ↔ `분리됨(물질)` | 2~4 | 1 `분리 주체·원인` → 개념·신격·국가·단체·종족·인물·아이템·사건<br>2 `분리 대상` → 개념·종족·아이템<br>3 `분리 결과물`(선택) → 개념·종족·아이템<br>4 `관련 공정·시설`(선택) → 개념·도시·아이템·사건·지형 |
| 832 | 2.12-4 | `변환함(물질)` ↔ `변환됨(물질)` | 3~4 | 1 `변환 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `변환 전 물질` → 개념·종족·아이템<br>3 `변환 후 물질` → 개념·종족·아이템<br>4 `관련 공정·수단`(선택) → 개념·아이템·사건 |
| 833 | 2.12-5 | `생산함` ↔ `생산됨` | 2~4 | 1 `생산 주체·시설` → 신격·국가·도시·단체·종족·인물·아이템<br>2 `생산물` → 개념·종족·아이템<br>3 `생산 원료`(선택) → 개념·종족·아이템<br>4 `관련 지역·시기`(선택) → 개념·국가·도시·사건·지형 |
| 834 | 2.12-5 | `제조함` ↔ `제조됨` | 2~4 | 1 `제조 주체·시설` → 신격·국가·도시·단체·종족·인물·아이템<br>2 `제조 대상` → 아이템<br>3 `제조 원료`(선택) → 개념·종족·아이템<br>4 `관련 공정·규격`(선택) → 개념·아이템·사건 |
| 835 | 2.12-5 | `조립함` ↔ `조립됨` | 2~4 | 1 `조립 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `조립 결과물` → 도시·아이템·지형<br>3 `구성 부품`(선택) → 아이템<br>4 `관련 설계·시설`(선택) → 개념·도시·아이템·지형 |
| 836 | 2.12-5 | `결합함` ↔ `결합됨` | 3~4 | 1 `결합 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `결합 대상 1` → 개념·신격·종족·인물·아이템<br>3 `결합 대상 2` → 개념·신격·종족·인물·아이템<br>4 `결합 결과물`(선택) → 개념·신격·종족·인물·아이템 |
| 837 | 2.12-5 | `복제함` ↔ `복제됨` | 3~4 | 1 `복제 주체` → 신격·국가·단체·종족·인물·아이템<br>2 `원본` → 개념·신격·종족·인물·아이템<br>3 `복제 결과물` → 개념·신격·종족·인물·아이템<br>4 `관련 기술·시설`(선택) → 개념·도시·아이템·지형 |
| 838 | 2.12-5 | `인쇄함` ↔ `인쇄됨` | 2~4 | 1 `인쇄 주체·시설` → 국가·도시·단체·종족·인물·아이템<br>2 `인쇄 대상·결과물` → 아이템<br>3 `원본·판형`(선택) → 아이템<br>4 `관련 시기`(선택) → 개념·사건 |
| 839 | 2.12-5 | `산출함` ↔ `산출됨` | 2~4 | 1 `산출 주체·지역·시설` → 국가·도시·단체·종족·인물·아이템·지형<br>2 `산출 자원·생산물` → 개념·종족·아이템<br>3 `관련 원천`(선택) → 개념·종족·아이템·지형<br>4 `관련 시기`(선택) → 개념·사건 |
| 840 | 2.12-5 | `재활용함` ↔ `재활용됨` | 2~4 | 1 `재활용 주체·시설` → 국가·도시·단체·종족·인물·아이템<br>2 `재활용 대상` → 아이템<br>3 `재활용 결과물`(선택) → 아이템<br>4 `관련 공정·사건`(선택) → 개념·아이템·사건 |
| 841 | 2.12-5 | `재사용함` ↔ `재사용됨` | 2~4 | 1 `재사용 주체` → 신격·국가·단체·종족·인물<br>2 `재사용 대상` → 도시·아이템·지형<br>3 `변경된 용도`(선택) → 개념<br>4 `관련 사건·시기`(선택) → 개념·사건 |

## 3. 집계

| 항목 | 수량 |
| --- | ---: |
| relation type | 55 |
| 대상 역할 슬롯 | 215 |
| 단일 카테고리 역할 | 38 |
| 복수 제한 카테고리 역할 | 177 |
| 전체 11개 허용 역할 | 0 |
| 2.1~2.12 누적 relation type | 841 |
| 2.1~2.12 누적 대상 역할 | 2479 |
