# 2.15 관계 대상 카테고리 매핑

> 상태: 범용 기준 확정 매핑  
> 범위: 시간·연대·순서 관계 9개와 대상 역할 27개  
> 적용 범위: `COMMON`

| 전역 ID | 세부 파트 | 관계 | 대상 수 | 대상 역할별 카테고리 |
| ---: | --- | --- | ---: | --- |
| 928 | 2.15-1 선행·후행 | `선행함(시간)` ↔ `후행함(시간)` | 2~3 | 1 `선행 대상` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>2 `후행 대상` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>3 `연대 근거`(선택) → 아이템 |
| 929 | 2.15-1 선행·후행 | `직전에 발생함` ↔ `직후에 발생함` | 2~3 | 1 `직전 사건` → 사건<br>2 `직후 사건` → 사건<br>3 `연대 근거`(선택) → 아이템 |
| 930 | 2.15-2 동시·동시대·기간 중첩 | `동시에 발생함` ↔ `동시에 발생함` | 2~3 | 1 `동시 사건 1` → 사건<br>2 `동시 사건 2` → 사건<br>3 `연대 근거`(선택) → 아이템 |
| 931 | 2.15-2 동시·동시대·기간 중첩 | `동시대임` ↔ `동시대임` | 2~3 | 1 `동시대 대상 1` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>2 `동시대 대상 2` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>3 `관련 시대`(선택) → 개념·사건 |
| 932 | 2.15-2 동시·동시대·기간 중첩 | `기간이 겹침` ↔ `기간이 겹침` | 2~3 | 1 `중첩 대상 1` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>2 `중첩 대상 2` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>3 `중첩 시기`(선택) → 개념·사건 |
| 933 | 2.15-3 기간·시대 포함 | `기간을 포함함` ↔ `기간에 포함됨` | 2~3 | 1 `포함 기간` → 개념·사건<br>2 `포함 대상` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>3 `연대 근거`(선택) → 아이템 |
| 934 | 2.15-3 기간·시대 포함 | `시대에 속함` ↔ `시대에 포함함` | 2~3 | 1 `시대 소속 대상` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>2 `소속 시대` → 개념·사건<br>3 `연대 근거`(선택) → 아이템 |
| 935 | 2.15-4 시작·종료 경계 | `시작점이 됨` ↔ `시작됨(시간)` | 2~3 | 1 `시작 기준` → 개념·사건<br>2 `시작 대상` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>3 `연대 근거`(선택) → 아이템 |
| 936 | 2.15-4 시작·종료 경계 | `종료점이 됨` ↔ `종료됨(시간)` | 2~3 | 1 `종료 기준` → 개념·사건<br>2 `종료 대상` → 개념·신격·국가·도시·단체·종족·인물·아이템·사건·설화·지형<br>3 `연대 근거`(선택) → 아이템 |

## 집계

| 항목 | 수량 |
| --- | ---: |
| relation type | 9 |
| 대상 역할 슬롯 | 27 |
| 단일 카테고리 역할 | 11 |
| 복수 제한 카테고리 역할 | 6 |
| 전체 11개 허용 역할 | 10 |
| 2.1~2.15 누적 relation type | 936 |
| 2.1~2.15 누적 대상 역할 | 2813 |
