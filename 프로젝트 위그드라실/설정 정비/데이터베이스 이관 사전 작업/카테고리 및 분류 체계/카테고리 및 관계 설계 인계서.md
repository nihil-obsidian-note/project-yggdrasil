# 카테고리 및 관계 설계 인계서

> 목적: 다른 GPT 대화에서 프로젝트 위그드라실의 문서 이관 설계를 중단 없이 이어가기 위한 현재 상태 요약이다.
>
> 상태: 카테고리 기준과 범용 관계 유형 v1의 936개 relation type이 확정되었다. 관계 대상 역할 2,813개의 카테고리 매핑도 확정했으며 실제 문서 데이터 이관과 테이블 구현은 아직 시작하지 않았다.

## 1. 이번 정리를 시작한 이유

기존 Markdown 문서는 `type`, `subtype`, 문서 폴더, `주요`, `일반` 값이 서로 다른 성격의 정보를 함께 담고 있었다. 이를 그대로 데이터베이스에 옮기면 문서 종류, 설정 중요도, 세계관 분류, 문서 용도가 뒤섞인다.

따라서 먼저 문서가 무엇에 관한 문서인지를 일관된 카테고리로 다시 정리했다. 그 다음에는 문서 링크만으로는 인물의 소속, 도시의 국가 귀속, 아이템의 소유자, 지역에 전해지는 설화처럼 의미 있는 사실을 검색·역추적할 수 없으므로, 방향과 의미를 가진 관계를 새로 설계하려 한다.

## 2. 확정된 문서 모델 방향

- 이관 대상은 `설정`, `신격`, `비밀 설정`이다.
- 세 문서군은 모두 하나의 `documents` 모델에 들어간다. `docType`은 사용하지 않는다.
- 비밀 설정은 문서 유형이 아니라 공개 범위 컬럼으로 제어한다.
- `importance`는 문서 관리·열람 중요도 컬럼이다.
- 문서 하나는 카테고리 하나만 선택한다. 복수 성격, 속성, 상태, 위계는 태그와 관계로 표현한다.
- `스토리 설계`, `설정 정비`, 템플릿, 지침, 라이브러리, 인덱스는 이번 이관 대상이 아니다.

## 3. 카테고리를 먼저 정리한 이유와 결과

카테고리는 검색·메뉴·문서 목록에서 문서의 기본 정체성을 판정하는 기준이다. 그래서 `일반`, `기타`, 빈값처럼 분류력이 없는 값을 노드로 만들지 않고, 문서를 부모 카테고리에 직접 연결한다.

- 카테고리는 월드별 독립 데이터다. 같은 이름이라도 월드가 다르면 별도 레코드가 된다.
- 하위 카테고리 전용 테이블은 만들지 않는다. `categories.upper_category_id`로 최대 3단계 계층을 만든다.
- 메인 카테고리는 개념, 신격, 국가, 도시, 단체, 종족, 인물, 아이템, 사건, 설화, 클래스, 지형, 용종이다.
- 클래스는 엘드로스 한정이다.
- 용종 개념 문서는 `개념`에 두고, 개별 용종 도감 문서는 `용종`과 계통 하위 카테고리에 둔다.

확정한 전체 매핑과 기존 메타데이터 이관 규칙은 다음 문서가 정본이다.

- [카테고리 이관 정리](카테고리%20및%20서브카테고리%20이관%20정리.md)

## 4. 관계를 새로 확립하려는 이유

문서 사이의 단순 링크는 연결 여부만 표현한다. 아래처럼 설정 사실 자체가 필요한 경우에는 구조화된 관계가 필요하다.

```text
인물 → 소속 → 단체
인물 → 출신 → 국가·도시·지역
도시·지형 → 위치함 → 국가·상위 지역
아이템 → 소유됨 → 인물·단체·국가
지역 → 전승함 → 설화
국가·단체 → 동맹·견제·적대 → 국가·단체
```

관계는 하나의 사실을 한 번만 저장하고, 대상 문서에서는 역관계로 조회한다. 예를 들어 인물의 `소속` 관계는 단체 문서에서 `소속 인물`로 보인다. 대칭 관계와 방향 관계를 구분하고, 관계 자체에 공개 범위·기간·설명·근거를 둘 계획이다.

범용 관계 유형 v1의 936개 relation type을 확정했다. 괄호형 독립 relation type을 포함한 대상 역할 2,813개는 범용 11개 메인 카테고리와 매핑했고, 룩스테라의 `용종` 및 엘드로스의 `클래스` 확장은 세계별 매핑으로 분리했다. 현재 이관 대기·재사용·통합·미결 항목은 아래 추적 문서를 기준으로 확인한다.

- [2.10 설화·전승·서사 관계 확정본](../범용%20관계%20유형/2.10.%20범용%20관계%20유형%20-%20설화·전승·서사%20관계%20확정본.md)
- [2.1~2.10 관계 대상 카테고리 매핑](../범용%20관계%20유형/2.1~2.10%20관계%20대상%20카테고리%20매핑.md)

- [범용 관계 유형 검토·이관 추적 문서](../검토%20및%20추적/범용%20관계%20유형%20검토·이관%20추적%20문서.md)

## 5. 다음 대화에서 이어갈 권장 순서

1. 관계 설계 초안과 2.1~2.10 확정본·추적 문서를 대조하여 2.11의 실제 범위를 재구성한다.
2. 후속 관계 파트를 모두 확정한 뒤 관계 유형·대상 역할·허용 카테고리의 테이블 구조를 확정한다.
3. 확정된 카테고리와 역할 매핑을 기준으로 현재 이관 대상 문서를 하나씩 매핑한다.
4. 이후에만 실제 데이터 이관과 애플리케이션 구현을 시작한다.

카테고리 구조 자체를 다시 바꿀 필요가 생기면 카테고리 이관 정리와 관계 대상 카테고리 매핑 문서를 함께 갱신한다.

## 6. 문서 위치 원칙

이 문서는 새 설정 정비 체계의 상세 이관 기준으로 복제한 자료다. 프로젝트 공통 정비와 이관 작업 기록은 중앙 `프로젝트 위그드라실/설정 정비/`에서 관리하며, 한 시리즈에만 적용되는 정비는 해당 시리즈의 `설정 정비/`에서 관리한다.

- 프로젝트 공통 정비 현황: `프로젝트 위그드라실/설정 정비/설정 정비 현황.md`
- 현재 활성 이관 작업: `프로젝트 위그드라실/설정 정비/데이터베이스 이관 사전 작업 정비.md`
- 설정 정본: `프로젝트 위그드라실/위그드라실/`, `프로젝트 위그드라실/룩스테라/`, `프로젝트 위그드라실/엘드로스/`
