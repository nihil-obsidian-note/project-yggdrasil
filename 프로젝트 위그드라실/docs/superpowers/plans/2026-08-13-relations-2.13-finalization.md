# Relations 2.13 Concepts Laws Effects Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finalize 45 new COMMON relation types for section 2.13 and synchronize every mapping, tracking, maintenance, and Superpowers record.

**Architecture:** One structured inventory generates the final relation document and COMMON mapping. SERIES extensions remain separate and are merged at application time; existing ID 836 is reused without duplicating COMMON data.

**Tech Stack:** Markdown, Node.js generation and verification scripts, Google Drive raw-file updates

## Global Constraints

- Relation instances have 2 to 4 participants.
- Every participant has a semantic role and an explicit COMMON category set.
- COMMON and SERIES mappings remain separate.
- New global IDs are exactly 842 through 886.
- Finalization updates setting-folder documents and the settings-only Superpowers registry as one set.
- No file is written to the TRPG data work folder.

---

### Task 1: Generate canonical 2.13 documents

**Files:**
- Create: `2.13. 범용 관계 유형 - 개념·법칙·영향 관계 확정본.md`
- Create: `2.13 관계 대상 카테고리 매핑.md`

- [x] Record 45 relation types with global IDs 842 through 886.
- [x] Record all semantic role slots and COMMON category mappings.
- [x] Record one reused relation and one integrated exclusion without assigning new IDs.

### Task 2: Synchronize SERIES and tracking documents

**Files:**
- Modify: `룩스테라 용종 관계 대상 확장 매핑.md`
- Modify: `엘드로스 클래스 관계 대상 확장 매핑.md`
- Modify: `범용·세계별 관계 대상 카테고리 매핑 적용 규칙.md`
- Modify: tracking, maintenance, handoff, and status documents

- [x] Add only semantically valid dragon and class roles.
- [x] Extend existing ID 836 for class targets and results without duplicating COMMON.
- [x] Resolve every 2.13 migration and hold candidate.
- [x] Move the next pointer to section 2.14 scope reconstruction.

### Task 3: Verify Drive deployment

**Files:**
- Create: `docs/superpowers/reports/2026-08-13-database-migration-relations-2.13-verification.md`
- Modify: `docs/superpowers/SUPERPOWERS SETTINGS REGISTRY.md`

- [x] Verify IDs, counts, role mappings, extension separation, and resolved candidates.
- [x] Upload new files and update existing files while preserving folder ownership.
- [x] Read back every changed Drive file and record completion in the settings registry.
