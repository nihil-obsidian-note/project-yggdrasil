# 데이터베이스 이관 관계 2.11·엘드로스 클래스 확정 세트 검증 보고서

> 상태: 검증 완료  
> 검증일: 2026-08-12  
> 실행 계획: `plans/2026-08-12-relations-2.11-and-eldros-class-finalization.md`

## 1. 검증 범위

2.11 신앙·권능·초월 확정본, 엘드로스 클래스 전용 확정본, 2.2 재분류 정정, 범용·세계별 대상 카테고리 매핑, 적용 규칙, 정비·추적·인계·중앙 현황 문서를 하나의 세트로 검증했다.

프로젝트 산출물은 12개 Markdown 정본과 이를 원래 경로 구조로 묶은 ZIP 1개다. Superpowers 실행 계획과 이 보고서는 ZIP에 포함하지 않고 Drive 지정 폴더에서 별도 관리한다.

## 2. 핵심 결과

| 항목 | 검증 결과 |
| --- | ---: |
| 2.2 활성 relation type | 67 |
| 2.11 구성 relation type | 34 |
| 2.11 기존 재분류 | 1 |
| 2.11 신규 COMMON relation type | 33 |
| 2.1~2.11 누적 COMMON relation type | 786 |
| 2.1~2.11 누적 대상 역할 | 2,264 |
| 엘드로스 클래스 전용 SERIES relation type | 7 |
| 엘드로스 클래스 전용 대상 역할 | 25 |
| 룩스테라 용종 허용 관계 | 674 |
| 룩스테라 용종 허용 대상 역할 | 1,240 |
| 엘드로스 클래스 기존 COMMON 허용 관계 | 74 |
| 엘드로스 클래스 기존 COMMON 허용 역할 | 80 |

## 3. 재분류 검증

`파문함 ↔ 파문당함`은 2.2의 요약 표·상세 정의·역할 집계에서 제거했고, 2.11-4의 전역 번호 55로 유지했다. 56~753은 재번호하지 않았으며 55도 다른 관계에 재사용하지 않는다.

2.2 상세 관계 제목은 1~67의 연속 번호로 정리했다. 2.1~2.10 대상 카테고리 매핑에서는 전역 번호 55를 제거하고 활성 관계 752개·대상 역할 2,139개로 정정했다.

## 4. 매핑 검증

2.11 관계 34개의 역할·카테고리 수를 행 단위로 대조했다. 대상 역할은 125개이며 단일 카테고리 33개, 복수 제한 92개, 전체 11개 허용 0개다.

룩스테라 용종은 2.11의 34개 관계·57개 역할을 추가 허용했다. 파문 관계의 기존 2개 허용 역할은 재분류 이동이므로 누적 증가는 신규 관계 33개·신규 허용 역할 55개다.

엘드로스 클래스는 2.11의 125개 역할에서 추가 허용 0개로 판정했다. 신앙·권능·신격 관계에 클래스를 억지로 넣지 않고, 각성·부여·박탈·상실·전직·사용 자격은 전용 SERIES 관계 7개로 분리했다.

## 5. 자동 검증

`node verify_relations_set.js` 실행 결과:

```json
{
  "status": "PASS",
  "files": 12,
  "commonRelations": 786,
  "commonRoles": 2264,
  "part211Relations": 34,
  "part211Roles": 125,
  "eldrosSeriesRelations": 7,
  "eldrosSeriesRoles": 25,
  "dragonAllowedRelations": 674,
  "dragonAllowedRoles": 1240,
  "errors": []
}
```

검사 항목은 파일 존재·비어 있지 않음, 관계 행 수, 연속 번호, 역할/카테고리 수 일치, 대상 수 2~4, 전역 번호 55·754~786, 파문 중복, 카테고리 집계, 세계별 허용 집계, 문서 간 786·2.12 참조, 플레이스홀더다.

## 6. ZIP 검증

- 파일명: `프로젝트 위그드라실_2.11_및_엘드로스_클래스_관계_확정본.zip`
- 포함 Markdown: 12개
- 원래 폴더 구조: 보존
- `unzip -t`: 오류 없음
- SHA-256: `32079ad5e0e87151e7cae7f0a75b2047d83f9238a7330ab8072a02188e16fb3e`

## 7. 최종 판정

2.11·엘드로스 클래스 확정 세트는 관계 정의, 재분류, 대상 카테고리 매핑, 세계별 확장, 적용 규칙, 정비·추적·인계·중앙 현황까지 동기화되었다.

다음 범용 관계 검토는 2.12 재료·구성·생산이며, 재활용·분리·채취·표본채취·표본화 후보를 관계 설계 초안과 기존 확정본에 대조하여 전체 리스팅하는 단계다.
